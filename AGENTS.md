# Agent Guidelines

Welcome to PromptForge! This is a Next.js local-first prompt consultant workspace.

## Best Practices
- Keep styling focused within `globals.css` using organized CSS variables.
- Optimize LLM APIs by supporting robust fallbacks.
- Ensure TypeScript types are updated in `src/lib/ai-result.ts`.
